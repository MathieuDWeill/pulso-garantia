# Pulso Garantia Streamlit Demo

Fast jury-facing demo app.

## Local run

cd streamlit_app
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py

## Deploy

Push the repo to GitHub, then deploy this folder on Streamlit Community Cloud.

Entry point:

streamlit_app/app.py
